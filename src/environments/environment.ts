// src/environments/environment.ts

export const environment = {
    production: false,
    // Base API URL for all service calls (Mock endpoint recommended)
    apiUrl: 'http://localhost:3000/api'
    // Example: Use JSON-Server or similar mock API for development
};
